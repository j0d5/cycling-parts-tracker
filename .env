DATABASE_TYPE=mariadb
DATABASE_NAME=cycling-parts-tracker
DATABASE_PATH=tmp/development.sqlite

# these variables aren't needed when using SQLite, but I keep them around for future use
DATABASE_USERNAME=
DATABASE_PASSWORD=
DATABASE_HOST=mariadb
DATABASE_PORT=3306
